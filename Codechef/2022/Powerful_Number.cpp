#include <bits/stdc++.h>
#define ll long long
using namespace std;

bool fact(ll n) {
    
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin >> t;

    while (t--)
    {
        ll n;
        cin >> n;
        ll count = 0;


        for (ll i = n; i >= 0; i--)
        {
            if(fact(n))
        }
        cout << count << endl;
    }
    return 0;
}